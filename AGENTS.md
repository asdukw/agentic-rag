# 工作约定

- 默认不运行测试。
- 默认不新增或修改测试；仅在用户明确要求时编写测试。
- 当用户尝试发版时，先询问是否需要运行测试；未经确认不执行测试。
- 完成一组逻辑完整的改动并完成适当验证后，应在合适的时机主动创建 commit；提交前确认不混入无关改动，并使用能准确描述变更的提交信息。用户明确要求暂不提交时除外。

# 架构原则

- 项目的核心是 Agentic：由 Agent 根据问题和当前状态自主决定检索什么、从哪里扩图、读取哪些证据以及何时停止；系统负责为 Agent 提供受控、可组合、可观测的相关检索、图扩展和证据读取等能力，不把某一种固定检索或扩图策略固化为项目核心。
